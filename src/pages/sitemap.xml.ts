import {pages,database,entityURL,game} from '../lib/data';
import type {APIContext} from 'astro';
export function GET({site}:APIContext){const origin=site||new URL('https://wanderburg.ymmyi.wiki');const paths=['/',...pages.filter(p=>!p.noindex).map(p=>'/'+p.slug+'/'),...Object.entries(database).flatMap(([t,entries])=>entries.map(e=>entityURL(t,e.slug)))];return new Response('<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">'+paths.map(p=>'<url><loc>'+new URL(p,origin).href+'</loc><lastmod>'+game.updated+'</lastmod></url>').join('')+'</urlset>',{headers:{'Content-Type':'application/xml'}});}
